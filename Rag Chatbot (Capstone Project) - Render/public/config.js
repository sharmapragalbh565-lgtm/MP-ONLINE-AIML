// API configuration
// When running locally or on Render (served together), use relative URL.
// When the frontend is deployed to Vercel separately, set this to your Render backend URL.
// Example: const API_BASE_URL = 'https://exoplanet-explorer.onrender.com';
// Empty string = relative URL: works on both localhost:8000 and Render (same-origin)
// Only change this if you deploy the frontend separately (e.g., Vercel).
const API_BASE_URL = '';

