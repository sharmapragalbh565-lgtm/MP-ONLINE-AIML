services:
  - type: web
    name: carlens-ai
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python src/train.py"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
